{
  "name": "darinlwebb.com",
  "scripts": {
    "dev": "nuxt",
    "generate": "nuxt generate",
    "deploy": "nuxt generate; push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^1.3.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1",
    "turretcss": "^4.1.0-beta4"
  }
}
